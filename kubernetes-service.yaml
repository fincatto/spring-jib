apiVersion: v1
kind: Service
metadata:
  name: spring-jib-service
spec:
  selector:
    app: spring-jib-app
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 8081
      targetPort: 8081
      name: management
  type: NodePort